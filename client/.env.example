# API base URL (leave empty to use the Vite dev proxy)
VITE_API_URL=http://localhost:5000/api/v1

# Google OAuth client id (same as the server's GOOGLE_CLIENT_ID)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
